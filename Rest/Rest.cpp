#include<Rest.h>
#include<string>
#include<map>

static bool Rest:create(string url,map<string,void *> map)
{

}

static string Rest:read(string url)
{
    
}

static bool Rest:update(string url,map<string,void *> map)
{
    
}

static bool Rest:cancel(string url)
{
    
}